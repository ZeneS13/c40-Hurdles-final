class Hurdle{
    constructor(x,y){
        this.x = x;
        this.y = y;
    }

    display(){

      image(hurdleImg,this.x,this.y,50,50);

        //  fill(0,100);
        // rect(this.x,this.y,50,50);
    }
}